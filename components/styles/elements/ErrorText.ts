import styled from 'styled-components/native';
import { space, SpaceProps } from 'styled-system';

export const ErrorText = styled.Text<SpaceProps>`
	${space}
	color: #ff4136;
	font-size: 12px;
	font-weight: bold;
`;
